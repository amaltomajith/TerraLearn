fastapi>=0.110.0
uvicorn>=0.28.0
python-dotenv>=1.0.0
httpx>=0.27.0
pydantic>=2.6.0
# langchain-mcp-adapters (Phase 5: web assistant <-> Farmer/Buyer MCP servers)
# hard-requires mcp<2.0.0 and langchain-core>=1.3.3 via its own package
# metadata (confirmed via `pip show`/importlib.metadata before pinning these,
# not guessed) — these floors exist to satisfy that, not arbitrarily bumped.
langchain>=1.3.0
langchain-core>=1.3.3
langchain-openai>=1.4.0
langgraph>=1.2.0
# Pinned exact, not >=: this ecosystem (mcp + langchain-mcp-adapters) has
# shown real cross-version incompatibility within this same project — mcp 2.x
# (this project's own earlier choice, Phases 1-4) turned out to be
# incompatible with langchain-mcp-adapters' mcp<2.0.0 requirement. Downgraded
# to 1.x's FastMCP (mcp.server.fastmcp.FastMCP, not
# mcp.server.mcpserver.MCPServer) — the dual-server mount recipe (lifespan
# sharing, TransportSecuritySettings) was re-verified working at this exact
# version before pinning it, not assumed unchanged from 2.x.
mcp==1.30.0
langchain-mcp-adapters==0.3.2
